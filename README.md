# bluej

Landing page blue•j — moda donna. Vite + React 19 + Tailwind CSS v4 + GSAP.

## Sviluppo

```bash
npm install
npm run dev      # http://localhost:5201
npm run build    # build di produzione in dist/
```

Deploy: Vercel rileva automaticamente Vite (build `npm run build`, output `dist`).
